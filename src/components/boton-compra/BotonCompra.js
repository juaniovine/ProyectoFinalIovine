import React from 'react'
import "./styles.css"

export default function BotonCompra() {
    // const BotonCompra = ({ titulo, funcion }) => {
    //     const handleClick = () => {
    //       // Realiza alguna acción al hacer clic en el botón, como agregar el producto al carrito.
    //       funcion(); // Llama a la función pasada como prop (en este caso, setCollection).
    //     };
      
        return (
            <div>
          <button>Agregar al carrito</button>
          </div>
        );
      }
    // }
